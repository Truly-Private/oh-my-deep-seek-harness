# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@truly-private/omdsh-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@truly-private/omdsh-lsp'
          - id: lsp-stdio
            name: '@truly-private/omdsh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@truly-private/omdsh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@truly-private/omdsh-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@truly-private/omdsh-llm-replay'
