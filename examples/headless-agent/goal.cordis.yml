# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@truly-private/omdsh-goal'
          - id: tool-goal
            name: '@truly-private/omdsh-tool-goal'
