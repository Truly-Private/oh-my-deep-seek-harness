# Test-only composition: create one goal through a Loader-mounted step consumer.
- id: cli-mock-llm
  name: '../cli-mock-llm.ts'

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@truly-private/omdsh-subprocess-local'

- id: bash
  name: '@truly-private/omdsh-bash-local'

- id: goal
  name: '@truly-private/omdsh-goal'
  config:
    defaultMaxGoalRounds: 11

- id: seed-goal
  name: './seed-goal.ts'

- id: agent-spine
  name: '@truly-private/omdsh-agent-spine-demo'
  config:
    agents:
      - id: main
        provider: cli-mock
        model: cli-mock
        cwd: !!js process.cwd()
    persona: 'Test the persisted goal domain.'
    workspaceContext: false

- id: persistence
  name: '@truly-private/omdsh-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: checkpoint-policy
  name: '@truly-private/omdsh-session-checkpoint-policy'
